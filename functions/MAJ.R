source("Functions.R")

#Actualisation (thème ou ID)
#actualiseTables(ids)
#actualiseTables(7)
#actualiseTables(Tableaux[Theme=="Commerce exterieur", ID])

#ids <- Tableaux[,ID]
#Tables <- lapply(ids, getTable)